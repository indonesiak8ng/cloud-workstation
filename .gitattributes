# Workflows carry shell scripts; CRLF would inject a stray \r on every line and
# break them when Actions runs them. Force LF for everything.
* text=auto eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.sh text eol=lf
*.md text eol=lf
